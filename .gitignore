# ---------------------------------------------------------------------------
# SENSITIVE DATA — never commit
# ---------------------------------------------------------------------------
# Real student reference sheets (names, CGPA, phone, email, resume links).
# These stay on the maintainer's machine only. See docs/PRIVACY.md.
Global/
private-data/
*.xlsx
*.xls
*.ods
!src-tauri/tests/fixtures/**/*.xlsx
# Anything a developer dumps while exploring real data
scratch/
*.real.json

# ---------------------------------------------------------------------------
# Build output
# ---------------------------------------------------------------------------
node_modules/
dist/
dist-ssr/
build/
target/
src-tauri/target/
src-tauri/gen/
*.local
# Xcode and Swift Package Manager output for the macOS widget. The generated
# NankivWidget.xcodeproj is committed on purpose, so building needs only Xcode.
.build/
.swiftpm/
widget-renders/

# ---------------------------------------------------------------------------
# Tooling
# ---------------------------------------------------------------------------
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.log
npm-debug.log*
coverage/
.nyc_output/
test-results/
playwright-report/

# ---------------------------------------------------------------------------
# Secrets — signing keys, release credentials
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.p12
*.keystore
tauri-signing-key*
*.asc

# Apple developer resources — licensed for local use only, never committed.
# SF Pro and SF Symbols may not be embedded or redistributed (see docs/DESIGN.md).
*.dmg
!src-tauri/target/**/bundle/dmg/*.dmg
